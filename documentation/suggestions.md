look at openrouter's design for their models tab. tyr to mirror this. integrate sidebars, interactable containers, especialyl for agent runs.
the results of weave's analysis for responses hould be accessible and opne. users should be able to easily compare models. they should be able 
to easily distinguish different task groups. really, everything should be clickable fro more ifnormation. right now only necessary containers/elemetns can be clcked.

for technicality, integrations from redis, copilotkit, and weave should be acessible, signposted, and technically deep, complex, and useful toward 
canopy. they should not be treated like side features, they are a core part of canopy itself. right now that looks unclear. 

users should be able to log in and have all functionalities of a real website. they should also be able to pause the simulation. remember, this is
a technically complex app, and the frontend needs to exemplify that. 

containers in the market flow visual. there are tree hydration issues that crash the entire simulation. further tabs other than just benchmark
should be added to introduce complexity and user accessibilty and UX into canopy. 

design is sitll kind of bad. maybe split up the main dashboard into multiple tabs, if possible. usemore professional fonts such as inter where posisble. let me know if any manual info must be entered.

labels for agents/workers are unclear and undescriptive (ex. job 001, worker shady). make this flexible and much more descriptive/userful to the user. the user should be able to clearly view the functionality of the app without having to click any buttons really. 