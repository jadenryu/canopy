for prizes: 
 
redis: prefers unique uses other than basic cacheing, and must be a specific redis use case (ex. cloud) rather than something simply "redis compatible" 
copilotkit: agui-focused (controlled, semi-open, open) 
weave: prefers usage of evaluation, newly released features, and multi-agent orchestrations that are unique and useful
 
maximize probability of winning a prize.